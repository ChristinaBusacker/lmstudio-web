diff --git a/apps/lmstudio-web/src/utils/typed-access.ts b/apps/lmstudio-web/src/utils/typed-access.ts
index 2e5f1c1..c7a3b91 100644
--- a/apps/lmstudio-web/src/utils/typed-access.ts
+++ b/apps/lmstudio-web/src/utils/typed-access.ts
@@
 export function isRecord(v: unknown): v is Record<string, unknown> {
-  return typeof v === 'object' && v !== null && !Array.isArray(v);
+  if (typeof v !== 'object' || v === null) return false;
+  if (Array.isArray(v)) return false;
+  if (v instanceof Date) return false;
+  if (v instanceof Error) return false;
+  return Object.getPrototypeOf(v) === Object.prototype;
 }